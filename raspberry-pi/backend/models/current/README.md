Place the active Hailo model bundle files here:

- manifest.json
- model.hef
- labels.txt or classes.json, optional

The backend validates manifest.json and the SHA-256 checksum of model.hef before it attempts inference.
