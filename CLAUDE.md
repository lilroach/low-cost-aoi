# 低成本 PCB AOI 系統開發規範 (CLAUDE.md)

本專案遵循 Andrej Karpathy 對 LLM 程式碼編寫缺陷的觀察，並要求所有 AI 助手在編寫、審查或重構此專案程式碼時，必須嚴格遵守以下四個核心原則。

---

## 核心行為指南 (Karpathy Guidelines)

### 1. 編碼前思考 (Think Before Coding)
**不要盲目假設。不要隱藏困惑。呈現權衡取捨。**
- 在實作之前，明確說明你的假設。如果不確定，請先詢問，不要猜測。
- 如果存在多種解釋，請同時呈現出來，不要默默做出選擇。
- 如果有更簡單的方法，請主動提出。在有必要時提出反對意見。
- 如果有任何不清楚的地方，請停下來，指出令人困惑之處，並要求澄清。

### 2. 簡潔優先 (Simplicity First)
**用最少的程式碼解決問題。不要進行過度推測。**
- 不要添加超出需求的功能。
- 不要為僅使用一次的程式碼建立抽象層。
- 不要添加未被要求的「彈性」或「可配置性」。
- 不要為不可能發生的場景撰寫錯誤處理。
- 如果你可以用 50 行寫完，就不要寫 200 行，請重新調整。
- **檢驗標準：** 資深工程師會覺得這太過複雜嗎？如果是，請簡化它。

### 3. 精準修改 (Surgical Changes)
**只修改必須修改的地方。只清理自己造成的影響。**
- 不要「順便改進」相鄰的程式碼、註解或格式。
- 不要重構沒有壞掉的東西。
- 配合現有的程式碼風格，即使你個人更偏好其他寫法。
- 如果你注意到無關的死程式碼，請提一下，但不要直接刪除。
- 刪除因你的修改而變得不再使用的 imports、變數或函式。不要刪除原本就存在的死程式碼，除非使用者要求。
- **檢驗標準：** 每一行被修改的程式碼，都必須能夠直接追溯到使用者的要求。

### 4. 目標驅動執行 (Goal-Driven Execution)
**定義成功標準。循環驗證直到達成目標。**
- 將陳述性任務轉化為可驗證的目標。
- 例如：「添加驗證」 → 「為無效輸入編寫測試，並使其通過」
- 例如：「修復 Bug」 → 「編寫能重現 Bug 的測試，並使其通過」
- 對於多步驟任務，請簡述一個計畫並逐步驗證。

---

## 專案特定技術規範

### 前端規範 (React + TypeScript)
- 僅使用函式型元件，不使用類別型元件。
- 單一元件只負責單一職責，呈現邏輯與商業邏輯分離。
- 自訂 Hook 必須以 `use` 開頭，不可在條件式或迴圈中呼叫。
- 命名規範：變數/函式使用 `camelCase`，元件名稱/檔案使用 `PascalCase`，常數使用 `UPPER_SNAKE_CASE`。
- 列表渲染必須提供穩定且唯一的 `key`。
- 註解需用正體中文說明「為什麼這樣設計」，而非重述程式碼本身。

### 後端規範 (Python FastAPI)
- Python 版本需 $\ge$ 3.10，所有函式與方法必須標註型別。
- 嚴格遵守分層架構：`api` (解析回應) -> `service` (商業邏輯) -> `repository` (資料存取) -> `schema`/`model`。
- 權限與安全檢查必須在 `service` 層完成。
- 禁止使用裸 `except`，禁止使用 `print` 作為日誌輸出（使用 `logging`）。
- 所有金鑰與密鑰必須從環境變數讀取。

---

## 語言與輸出約定
- 所有回覆、說明、註解、文件必須使用**正體中文（符合台灣用語）**。
- 程式碼中的識別字（變數、函式、類別等）一律使用**英文**。
